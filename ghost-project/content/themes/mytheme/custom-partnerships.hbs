{{!< default}}

{{#post}}
<article class="partnerships-page-wrapper {{post_class}}">
    
    {{!-- Premium Parallax Hero --}}
    <header class="partners-parallax-hero">
        <div class="parallax-bg" style="background-image: url('{{#if feature_image}}{{img_url feature_image}}{{else}}{{asset "images/about-hero.jpg"}}{{/if}}');"></div>
        <div class="partners-hero-overlay"></div>
        <div class="container partners-hero-content reveal">
            <span class="hero-label-gold">{{#if custom_excerpt}}{{custom_excerpt}}{{else}}Strategic Collaboration{{/if}}</span>
            <h1 class="partners-title-large">{{title}}</h1>
            <p class="partners-subtitle-large">{{excerpt}}</p>
        </div>
        <div class="hero-wave">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320" preserveAspectRatio="none">
                <path fill="#ffffff" fill-opacity="1" d="M0,224L48,213.3C96,203,192,181,288,181.3C384,181,480,203,576,224C672,245,768,267,864,250.7C960,235,1056,181,1152,149.3C1248,117,1344,107,1392,101.3L1440,96L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path>
            </svg>
        </div>
    </header>

    <div class="container partners-main">
        
        {{!-- Iconic Value Grid --}}
        <section class="partnership-values-section reveal">
            <div class="partners-value-grid">
                {{#get "posts" filter="tag:hash-partnership-value" order="published_at asc" limit="3"}}
                    {{#foreach posts}}
                    <div class="value-card-premium {{#if @first}}card-active{{/if}}">
                        {{#if custom_excerpt}}
                        <div class="value-icon-box">{{{custom_excerpt}}}</div>
                        {{/if}}
                        <div class="value-text-box">
                            <h3>{{title}}</h3>
                            <p>{{excerpt}}</p>
                        </div>
                    </div>
                    {{/foreach}}
                {{/get}}
            </div>
        </section>

        {{!-- Partnership Marquee (Even if placeholders, looks premium) --}}
        <section class="partnership-marquee-section reveal reveal-left">
            <h4 class="marquee-title">Trust & Collaboration</h4>
            <div class="marquee-container">
                <div class="marquee-track">
                    {{#get "tags" filter="visibility:internal+slug:~^hash-partner-type" limit="10"}}
                        {{#foreach tags}}
                            <div class="marquee-item text-logo">{{description}}</div>
                        {{/foreach}}
                        {{!-- Duplicated for smooth loop --}}
                        {{#foreach tags}}
                            <div class="marquee-item text-logo">{{description}}</div>
                        {{/foreach}}
                    {{/get}}
                </div>
            </div>
        </section>

        {{!-- Collaboration Block --}}
        <section class="collaboration-types reveal">
            <div class="collab-grid">
                <div class="collab-info">
                    {{#get "pages" slug="partnership-collab-info" limit="1"}}
                        {{#foreach pages}}
                            <h2 class="section-title">{{title}}</h2>
                            <p class="section-text">{{excerpt}}</p>
                        {{/foreach}}
                    {{else}}
                        <h2 class="section-title">Who can Partner with Us?</h2>
                        <p class="section-text">Join a mission-driven ecosystem dedicated to serving humanity with efficiency and compassion.</p>
                    {{/get}}
                </div>
                <div class="collab-list">
                    {{#get "posts" filter="tag:hash-collab-type" order="published_at asc" limit="5"}}
                        {{#foreach posts}}
                        <div class="collab-item">
                            <div class="collab-num">0{{@number}}</div>
                            <div class="collab-body">
                                <strong>{{title}}</strong>
                                <p>{{excerpt}}</p>
                            </div>
                        </div>
                        {{/foreach}}
                    {{/get}}
                </div>
            </div>
        </section>

    </div>

    {{!-- High-End CTA Section --}}
    <section class="premium-cta-block reveal">
        <div class="container text-center">
            {{#get "pages" slug="partnership-cta" limit="1"}}
                {{#foreach pages}}
                <div class="cta-glass-card">
                    <h2>{{title}}</h2>
                    <p>{{excerpt}}</p>
                    <div class="cta-actions">
                        <a href="{{#if custom_excerpt}}{{custom_excerpt}}{{else}}mailto:foundationdalailama@gmail.com{{/if}}" class="btn-premium-gold">Contact Leadership</a>
                    </div>
                </div>
                {{/foreach}}
            {{/get}}
        </div>
    </section>

    {{!-- Original Page Content (for reference/editor content) --}}
    <section class="original-content-section hidden">
        <div class="container post-content-body">
            {{content}}
        </div>
    </section>

</article>
{{/post}}
